# .github/workflows/build-and-sign.yml

name: 🚀 Build, Package e Rilascio Manuale (Con Firma FIX)

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Numero Versione (es. 2.1.0)'
        required: true
        default: '2.0.0'

env:
  RELEASE_VERSION: ${{ github.event.inputs.version }}

permissions:
  contents: write

jobs:
  # ===========================
  # WINDOWS BUILD + SIGNING
  # ===========================
  build_windows:
    runs-on: windows-latest
    steps:
    - name: ⬇️ Checkout Repository
      uses: actions/checkout@v4
      
    - name: 🐍 Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.13' 

    - name: 📦 Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    - name: 🔐 Restore Secret JSON
      shell: bash
      env:
        SECRET_CONTENT: ${{ secrets.CLIENT_SECRET_JSON }}
      run: |
        python -c "import os; open('cloud_utils/client_secret.json', 'w', encoding='utf-8').write(os.environ['SECRET_CONTENT'])"

    - name: 🔨 Run PyInstaller (Windows)
      run: pyinstaller --clean SaveState-OneFile.spec

    # --- STEP AGGIUNTO: Carichiamo il file non firmato per avere l'ID ---
    - name: 📤 Upload Unsigned Artifact
      id: upload_unsigned_step  # Questo ID ci serve per il passaggio dopo
      uses: actions/upload-artifact@v4
      with:
        name: Unsigned-SaveState
        path: dist/SaveState.exe
        if-no-files-found: error
        retention-days: 1

    # --- STEP CORRETTO: Ora usiamo l'ID generato sopra ---
    - name: 🔏 Sign Artifact with SignPath (TEST)
      uses: signpath/github-action-submit-signing-request@v1
      with:
        api-token: '${{ secrets.SIGNPATH_API_TOKEN }}'
        organization-id: '${{ secrets.SIGNPATH_ORGANIZATION_ID }}'
        project-slug: 'SaveState'
        signing-policy-slug: 'test-signing' 
        artifact-configuration-slug: 'initial'
        # AL POSTO DI ARTIFACT-FILE, USIAMO L'ID:
        github-artifact-id: '${{ steps.upload_unsigned_step.outputs.artifact-id }}'
        github-token: '${{ secrets.GITHUB_TOKEN }}'
        wait-for-completion: true
        output-artifact-directory: 'dist_signed'

    - name: 🗄️ Create Release Zip Package
      shell: powershell
      run: |
        $zipName = "SaveState_v${{ env.RELEASE_VERSION }}_Win.zip"
        mkdir release_pack
        # Prendiamo il file firmato dalla cartella di output di SignPath
        copy dist_signed\SaveState.exe release_pack\
        copy LICENSE release_pack\
        copy README.md release_pack\
        Compress-Archive -Path release_pack\* -DestinationPath $zipName
        
    - name: ⬆️ Upload Windows Package
      uses: actions/upload-artifact@v4
      with:
        name: SaveState-Windows-Package
        path: SaveState_v${{ env.RELEASE_VERSION }}_Win.zip

  # ===========================
  # LINUX BUILD (Nessuna modifica)
  # ===========================
  build_linux:
    runs-on: ubuntu-latest
    steps:
    - name: ⬇️ Checkout Repository
      uses: actions/checkout@v4
      
    - name: 🐍 Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.13'

    - name: 📦 Install Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y python3-dev
        python3 -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    - name: 🔐 Restore Secret JSON
      shell: bash 
      env:
        SECRET_CONTENT: ${{ secrets.CLIENT_SECRET_JSON }}
      run: |
        python -c "import os; open('cloud_utils/client_secret.json', 'w', encoding='utf-8').write(os.environ['SECRET_CONTENT'])"

    - name: 🔨 Run PyInstaller (Linux)
      run: pyinstaller --clean SaveState-OneFile-Linux.spec

    - name: 🗄️ Create Release Tarball Package
      run: |
        DIR_NAME="SaveState_v${{ env.RELEASE_VERSION }}_Linux"
        mkdir -p "$DIR_NAME"
        cp -r dist/SaveState* "$DIR_NAME/"
        cp LICENSE "$DIR_NAME/"
        cp README.md "$DIR_NAME/"
        tar -czvf "SaveState_v${{ env.RELEASE_VERSION }}_Linux.tar.gz" "$DIR_NAME"

    - name: ⬆️ Upload Linux Package
      uses: actions/upload-artifact@v4
      with:
        name: SaveState-Linux-Package
        path: "SaveState_v${{ env.RELEASE_VERSION }}_Linux.tar.gz"

  # ===========================
  # RELEASE CREATION
  # ===========================
  create_release:
    needs: [build_windows, build_linux]
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout Code
        uses: actions/checkout@v4

      - name: ⬇️ Download all artifacts
        uses: actions/download-artifact@v4

      - name: 🚀 Create Release with GitHub CLI
        env:
          GH_TOKEN: ${{ secrets.MY_RELEASE_TOKEN }}
        run: |
          echo "Creazione Release v${{ env.RELEASE_VERSION }}..."
          gh release create v${{ env.RELEASE_VERSION }} \
            SaveState-Windows-Package/* \
            SaveState-Linux-Package/* \
            --title "SaveState v${{ env.RELEASE_VERSION }}" \
            --draft \
            --generate-notes