# .github/workflows/build-and-sign.yml

name: 🚀 Build, Package e Rilascio Manuale

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Numero Versione (es. 2.1.0)'
        required: true
        default: '2.0.0'

env:
  RELEASE_VERSION: ${{ github.event.inputs.version }}

jobs:
  # ===========================
  # WINDOWS BUILD
  # ===========================
  build_windows:
    runs-on: windows-latest
    steps:
    - name: ⬇️ Checkout Repository
      uses: actions/checkout@v4
      
    - name: 🐍 Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.13' 

    - name: 📦 Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    # --- 🔐 MAGIA QUI SOTTO ---
    # Questo passaggio crea il file client_secret.json prendendo il contenuto dai Secrets di GitHub
    - name: 🔐 Restore Secret JSON
      shell: powershell
      env:
        SECRET_CONTENT: ${{ secrets.CLIENT_SECRET_JSON }}
      run: |
        # Crea il file usando il contenuto della variabile segreta
        $env:SECRET_CONTENT | Out-File -FilePath cloud_utils\client_secret.json -Encoding utf8
        Write-Host "File segreto ripristinato temporaneamente per la build!"

    - name: 🔨 Run PyInstaller (Windows)
      run: pyinstaller --clean SaveState-OneFile.spec

    - name: 🗄️ Create Release Zip Package
      shell: powershell
      run: |
        $zipName = "SaveState_v${{ env.RELEASE_VERSION }}_Win.zip"
        mkdir release_pack
        copy dist\SaveState.exe release_pack\
        copy LICENSE release_pack\
        copy README.md release_pack\
        Compress-Archive -Path release_pack\* -DestinationPath $zipName
        
    - name: ⬆️ Upload Windows Package
      uses: actions/upload-artifact@v4
      with:
        name: SaveState-Windows-Package
        path: SaveState_v${{ env.RELEASE_VERSION }}_Win.zip

  # ===========================
  # LINUX BUILD
  # ===========================
  build_linux:
    runs-on: ubuntu-latest
    steps:
    - name: ⬇️ Checkout Repository
      uses: actions/checkout@v4
      
    - name: 🐍 Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.13'

    - name: 📦 Install Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y python3-dev
        python3 -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    # --- 🔐 MAGIA ANCHE PER LINUX ---
    - name: 🔐 Restore Secret JSON
      env:
        SECRET_CONTENT: ${{ secrets.CLIENT_SECRET_JSON }}
      run: |
        echo "$SECRET_CONTENT" > cloud_utils/client_secret.json
        echo "File segreto ripristinato!"

    - name: 🔨 Run PyInstaller (Linux)
      run: pyinstaller --clean SaveState-OneFile-Linux.spec

    - name: 🗄️ Create Release Tarball Package
      run: |
        DIR_NAME="SaveState_v${{ env.RELEASE_VERSION }}_Linux"
        mkdir $DIR_NAME
        cp dist/SaveState $DIR_NAME/
        cp LICENSE $DIR_NAME/
        cp README.md $DIR_NAME/
        tar -czvf SaveState_v${{ env.RELEASE_VERSION }}_Linux.tar.gz $DIR_NAME

    - name: ⬆️ Upload Linux Package
      uses: actions/upload-artifact@v4
      with:
        name: SaveState-Linux-Package
        path: SaveState_v${{ env.RELEASE_VERSION }}_Linux.tar.gz

  # ===========================
  # RELEASE CREATION
  # ===========================
  create_release:
    needs: [build_windows, build_linux]
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Download all artifacts
        uses: actions/download-artifact@v4

      - name: 🏷️ Create GitHub Release Draft
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ env.RELEASE_VERSION }}
          name: SaveState v${{ env.RELEASE_VERSION }}
          draft: true
          body: |
            ## Novità di SaveState v${{ env.RELEASE_VERSION }}
            (Scrivi qui il changelog...)
          files: |
            SaveState-Windows-Package/*
            SaveState-Linux-Package/*